
export const GlobalPrefs = {
    dateFormat:'dddd, MM DD YYYY',
    dateOnlyFormat:'YYYY-MM-DD',
    paginationLimit: 30,
    fuse: {
        options: {
            threshold: 0,
            location: 0,
            distance: 0,
            maxPatternLength: 32,
            minMatchCharLength: 2,
        }
    }
}
